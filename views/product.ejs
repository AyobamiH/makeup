<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    
    <link rel="stylesheet" href="css/style.css">
    <link href="https://cdn.jsdelivr.net/npm/daisyui@2.31.0/dist/full.css" rel="stylesheet" type="text/css" />
<script src="https://cdn.tailwindcss.com"></script>
    <title>ToDo-PoMoDoRo</title>
</head>




          
    <div class="hero min-h-screen bg-base-200">
        <div class="hero-content  ">
            

 
                <ul class="h-full">
                    <% Array.from(products).forEach( product => { %>
                            <li class='todoItem h-full p-8 w-2/5 ' data-id='<%=product.id%>'>
                                <p class='  <%= product.api_featured_image === true ? 'product.name' : 'not'%>'><strong>Name:</strong><%= product.name %></p>
                               <img class="h-32 w-1/3" src="<%= product.api_featured_image %>" alt="">
                            
                                <p > <strong> Product Description:</strong><%= product.description %></p>
                                <p ><strong> Brand Name:</strong> <%= product.brand %></p>
                                <p class='<%= product.price_sign  === true && product.price === true && product.currency === true ? 'product.price_sign product.price product.currency' : 'NOT AVAILABLE'%>'><strong>Price:</strong> <%= product.price_sign  %><%= product.price  %><%= product.currency  %></p>
                                <a target="_blank" href="<%= product.product_link %>" id="productSearch"  type="submit" class="btn btn-primary">Buy Now</a>
                            </li>
                    <% }) %>    
                </ul> 
      


        </div>


      
    
    </div>

    
    <!-- Jumbotron -->
  </header>




   
    
  
 
</html>